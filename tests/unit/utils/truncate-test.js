import truncate from 'example/utils/truncate';
import { module, test } from 'qunit';

module('Unit | Utility | truncate', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = truncate();
    assert.ok(result);
  });
});
